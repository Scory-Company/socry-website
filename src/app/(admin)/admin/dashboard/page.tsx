import PortalDashboardPlaceholder from "@/components/portal/PortalDashboardPlaceholder"

export default function AdminDashboard() {
  return (
    <PortalDashboardPlaceholder
      title="Admin dashboard is ready for API wiring"
      description="This shell is intentionally minimal. Next step is connecting the real admin auth API and then layering dashboard modules back in one by one."
    />
  )
}
