import PortalDashboardPlaceholder from "@/components/portal/PortalDashboardPlaceholder"

export default function AuthorDashboard() {
  return (
    <PortalDashboardPlaceholder
      title="Reviewer dashboard is ready for API wiring"
      description="This reviewer area has been reduced to the essentials so the upcoming role-based auth API can be integrated without legacy UI noise."
    />
  )
}
